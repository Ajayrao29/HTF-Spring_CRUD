server:
  port: 8080
spring:
  application:
    name: product
  datasource:
    url: jdbc:h2:mem:mydb1
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

